# 4W1F 3.3.0 – Release Checklist

Status: **prepared, not installed**

## Included
- Flexible image publishing: profile / gallery / private
- Instagram-style member profiles
- Owner role and function-label management
- Social Media Studio drafts prepared for later Meta API integration
- Crew Live refresh + Life360-style active-member presentation
- Smart map inputs: place name search auto-centers creation maps and sets a draggable exact pin for events and announcements
- Full Crew Project CRUD
- Owner/Admin support tickets with chat, private image attachments and push queue
- Role-aware "What's new?" release notes

## Release files
- `update-3.3.js`
- `index.html` -> version/build + script include
- `sw.js` -> cache version + update script
- `version.json` -> 3.3.0 / 2026-09-23.1
- `release/3.3.0-backend-finalize.sql`

## Checks completed
- update-3.3.js parses successfully
- smart map overrides for event/announcement creation parse and initialize with the prepared feature pack
- update-3.3.js initializes with stubbed production globals without ReferenceError
- sw.js parses successfully
- version.json parses successfully
- backend finalize SQL executed successfully inside BEGIN/ROLLBACK (syntax/prerequisites checked without persistence)
- Support RLS probe (rolled back):
  - ticket creator sees own Admin + Owner tickets
  - another admin sees Admin ticket but not Owner ticket
  - Owner sees both
- main branch remains on 3.2.4 and does not load update-3.3.js

## Final install order
1. Apply `release/3.3.0-backend-finalize.sql` to production.
2. Run Supabase security/performance advisor check.
3. Merge `prepare/3.3.0` into `main`.
4. Confirm GitHub Pages / service worker serves 3.3.0.
5. Smoke-test Owner, Admin and Member paths.
6. Confirm support push on a real subscribed device.
